-   Unit « extract_layer » : On fait une sélection par index de la première dimension. Ce n’est pas robuste du tout. Je propose qu’on utilise array pour cela. Ça implique une fonction non compilée et d’avoir le nom de la dimension qu’on veut sélectionner. J’imagine qu’il faut des noms de dimensions standards pour ça ? T, X, Y, Z pour la 4D.
-   Toujours dans « extract_layer » tu utilise le nom 2d_forcing et 3d_forcing . Est-ce que c’est générique ? Les unité de calcul doivent être générique donc je me demande si la définition de Unit est encore pertinente. Peut-être que je n’ai pas compris ce que fait la classe ? Est-ce que le code ajouté récemment permet d’avoir n’importe quel forçage ?
